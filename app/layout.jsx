export const metadata = { title: 'ClasificaAI', description: 'Clasificación inteligente de productos aduaneros' };
export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>{children}</body>
    </html>
  );
}